#include <stdio.h>
#include <stdlib.h>
#include "mysh.h"

int main(int argc, char **argv) {
    char *word, **args, n;
    while(1) {
        printf("mysh> ");
        fflush(stdin);
        while(n = getchar() != '\n') {

        }
    }
}